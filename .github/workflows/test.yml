---
name: Test

on:
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.11', '3.12', '3.13', '3.14']
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      # Runs the end-to-end tests of stix_create.py and stix_create_v2.py (tests/) on every Python version
      - name: Test
        uses: ./.github/actions/test
        with:
          python-version: ${{ matrix.python-version }}
